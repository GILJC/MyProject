마이크로소프트 스토어에 들어가서
터미널 검색
Windows Terminal 다운, 설치

기본으로 3개 깔려있는데,
여기에 Anaconda 추가해서 쓰면 됨.


설정 들어가면
좌측 하단에 Json 파일 열기  <-  클릭
(여기서 Atom 설치 안되어있으면 notepad=txt 로 실행되는데)
(난 Atom 설치해놔서  Atom으로 실행시킴)


약간 아래에
list
안에 3개의 묶음 {} 이 있는데,

cmd 부분 중괄호{}
통째로 복사해서,

콤마, 붙이고 뒤에 추가.

guid 맨뒤에 자릿수는 그대로 하고, 중복 안되게, 숫자 몇개 바꿔줌

name은 Anaconda로 변경

Anaconda Powershell Prompt (anaconda3) 바로가기 우클릭, 
대상의 내용 전부 복사

그리고
commandline 에 전부 붙여넣기.
대충 이런식으로 됨

"commandline": "%windir%\\System32\\WindowsPowerShell\\v1.0\\powershell.exe -ExecutionPolicy ByPass -NoExit -Command \"& 'C:\\Users\\Administrator\\anaconda3\\shell\\condabin\\conda-hook.ps1' ; conda activate 'C:\\Users\\Administrator\\anaconda3' \"",

여기서 \부분을 전부 2개씩으로 바꿔주고(\\)
큰따옴표 앞에 \ 하나씩 붙여주기(총 2개일듯)
완성하면 이런식이 됨
"commandline": "%windir%\\System32\\WindowsPowerShell\\v1.0\\powershell.exe -ExecutionPolicy ByPass -NoExit -Command \"& 'C:\\Users\\Administrator\\anaconda3\\shell\\condabin\\conda-hook.ps1' ; conda activate 'C:\\Users\\Administrator\\anaconda3' \"",
(위에건 완성한거...원본은 \1개임)


이제 저장하고 닫으면 됨.
저장했을때, 에러 안뜨면 제대로 된것.
제대로 안됐으면 에러 메세지 뜸.

windows terminal  실행해서
상단의 아래화살표 누르면 Anaconda 추가되어있음.


[아이콘 변경]
아나콘다 바로가기 우클릭 -> 아이콘 변경 클릭 -> 위의 text 그대로 복사

다시 windows terminal 들어가서 -> 설정 -> Anaconda 클릭 -> 아이콘 변경 부분에 그대로 붙여넣기
저장.

이제 닫고 잘 쓰면 됨


(참고로 바로가기 위치는 따로 안빼놨으면 찾기 어려울듯.)
(시작메뉴에는 이렇게 등록되어있음)
(C:\Users\Administrator\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Anaconda3 (64-bit))
(참고, 아나콘다 설치 위치)
(C:\Users\Administrator\anaconda3)


[윈도우 태마 변경]


다시 윈도우 터미널 설정 Json 파일 실행


인터넷에서
window terminal theme 검색

https://windowsterminalthemes.dev/ 접속

원하는 태마 골라서
Get theme
클릭하면 복사됨

Json파일에서

"schemes"{}
안에 콤마, 넣고 추가

여기서 name은 복사해둠

그리고 나서 profiles의 default에 colorScheme를 설정.
복사해둔 이름 붙여넣기.

"profiles": {
	"defaults": {
		"colorScheme": "Snazzy",
		"fontSize": 10,
		"fontFace": "Monaco"
	},

대충 이런 식으로 됨

이제 잘 쓰면 됨



[][]
참고로 이걸로(window terminal)
putty도 됨




[][][]  +@
default에

"cursorShape": "vintage",

요거 넣어주면, 커서박스(텍스트 입력부분에 박짝이는 사각형)가 녹색으로 반짝이고, 밑줄이 아니라 사각형으로 커짐

->

빈티지로 하면 안 깜빡이고 
"cursorShape": "filledBox"
요거로 해야 깜빡인다고 함


[이라곤 하는데, 아이콘 변경만 했고]
[난 테마, 커서변경은 안함]